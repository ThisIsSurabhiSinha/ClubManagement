document.getElementById("joinMeeting").addEventListener("click", joinMeeting);
document.getElementById("startTest").addEventListener("click", startTest);
function joinMeeting() {
    // Implement logic to join a live meeting (e.g., redirect to a meeting platform).
    alert("Redirecting to the live meeting platform...");
}
function startTest() {
    // Implement logic to start a test or assessment.
    alert("Starting the test...");
}